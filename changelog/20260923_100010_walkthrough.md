# Walkthrough: Bổ Sung API & Function `get_form_claim_chi_phi_forward_data_internal_sign`

## Thông tin tổng quan
- **Thời gian thực hiện**: 23/09/2026 10:00:10 (Giờ địa phương)
- **Tài liệu PRD cập nhật**: [prd_form_claim_chi_phi.md](file:///D:/ai-docs/prd/prd_form_claim_chi_phi.md)
- **Stored Function PostgreSQL**:
  - [public/functions/get_form_claim_chi_phi_forward_data_internal_sign.sql](file:///D:/postgres/public/functions/get_form_claim_chi_phi_forward_data_internal_sign.sql)
  - [get_form_claim_chi_phi_forward_data_internal_sign.sql](file:///D:/postgres/get_form_claim_chi_phi_forward_data_internal_sign.sql)
- **Mục tiêu**: Bổ sung đặc tả API vào tài liệu PRD hệ thống Claim chi phí và lập trình Stored Function PostgreSQL trung gian (Forward API) để lấy dữ liệu tiến độ ký số và file đính kèm từ eOffice. Không ràng buộc kiểm tra validation bắt buộc với `manv` hay `ky_chi_phi`.

---

## 1. Chi tiết bổ sung vào tài liệu PRD (`prd_form_claim_chi_phi.md`)

Tại Mục **6.5 (Nhóm Công tác phí & Báo cáo)**, đã bổ sung và hoàn thiện đặc tả cho API:
- **Tên Function**: `get_form_claim_chi_phi_forward_data_internal_sign`
- **Loại**: READ (Forward Request đến eOffice API)
- **Standard**: Tuân thủ tuyệt đối `write_get_function.md`.
- **Logic xử lý**:
  1. **Parse Input Parameters (Không bắt buộc check dữ liệu)**:
     - `manv`: Mã nhân viên (tùy chọn, nếu truyền thì lọc theo nhân viên, không truyền thì lấy tất cả hồ sơ ký số trong kỳ).
     - `ky_chi_phi`: Kỳ chi phí kế toán (tùy chọn, hỗ trợ định dạng `YYYY-MM`, `YYYY-MM-DD`, hoặc `timestamp` ISO; nếu không truyền thì tự động lấy tháng hiện tại). Không chặn bắt lỗi validation.
  2. **Tự động quy đổi ngày đầu và cuối tháng**:
     - `date_start`: `TO_CHAR(date_trunc('month', v_date), 'YYYY-MM-DD')` (Ví dụ: `2026-10-01`).
     - `date_end`: `TO_CHAR((date_trunc('month', v_date) + interval '1 month - 1 day')::date, 'YYYY-MM-DD')` (Ví dụ: `2026-10-31`).
  3. **Forward Request đến eOffice**: Gọi helper `public.get_python_eo_data` kết nối endpoint `https://eoffice.meraplion.com/admincp/api/api/raw/data-internal-sign` với các tham số `date_start`, `date_end`, `user_code: manv` (nếu có), `limit: 1000`.
  4. **Lọc và Tinh gọn Output**: Chỉ lấy mảng danh sách biểu mẫu với các trường cần thiết theo đúng yêu cầu:
     - `form_code`: Mã biểu mẫu (`BMKT005-DNTTCTP`, `BMKT013-KH-TH-CP`, `BMKT002-DNTT`).
     - `form_name`: Tên biểu mẫu.
     - `created_at`: Thời gian tạo hồ sơ ký số.
     - `updated_at`: Thời gian cập nhật trạng thái mới nhất.
     - `created_code`: Mã nhân viên lập biểu mẫu.
     - `created_name`: Tên nhân viên lập biểu mẫu.
     - `current_step_status`: Trạng thái bước duyệt/ký hiện tại (`pending`, `approved`...).
     - `attachment_name`: Tên file đính kèm/ký hoàn tất.
     - `attachment_url`: Link tải file ký số hoàn tất.

---

## 2. Chi tiết Stored Function PostgreSQL (`get_form_claim_chi_phi_forward_data_internal_sign`)

Function được viết bằng `plpgsql`, tuân thủ chuẩn `write_get_function.md`, không chặn validation bắt buộc và tái sử dụng helper `public.get_python_eo_data` sẵn có trên database:

```sql
-- FUNCTION: public.get_form_claim_chi_phi_forward_data_internal_sign
-- Language: plpgsql
-- Arguments: url_param jsonb

CREATE OR REPLACE FUNCTION public.get_form_claim_chi_phi_forward_data_internal_sign(url_param jsonb)
 RETURNS jsonb
 LANGUAGE plpgsql
AS $function$
/* 
Function: get_form_claim_chi_phi_forward_data_internal_sign
Mục đích: Lấy dữ liệu trình ký nội bộ từ eOffice theo kỳ chi phí và mã nhân viên.
Tự động chuyển kỳ chi phí thành ngày đầu và ngày cuối tháng để gửi API sang eOffice.
Không chặn validation bắt buộc mã nhân viên hay kỳ chi phí.
*/
DECLARE
    p_manv text := COALESCE(url_param->>'manv', '');
    p_ky_chi_phi text := COALESCE(url_param->>'ky_chi_phi', '');
    v_date date;
    v_date_start text;
    v_date_end text;
    v_params jsonb;
    v_raw_response jsonb;
BEGIN
    /* 1. Tự động quy đổi kỳ chi phí sang ngày đầu tháng (v_date_start) và cuối tháng (v_date_end).
          Nếu không truyền thì mặc định lấy tháng hiện tại. */
    IF length(p_ky_chi_phi) = 7 THEN
        v_date := (p_ky_chi_phi || '-01')::date;
    ELSIF length(p_ky_chi_phi) >= 10 THEN
        v_date := (SUBSTRING(p_ky_chi_phi FROM 1 FOR 10))::date;
    ELSE
        v_date := CURRENT_DATE;
    END IF;

    v_date_start := TO_CHAR(date_trunc('month', v_date), 'YYYY-MM-DD');
    v_date_end := TO_CHAR((date_trunc('month', v_date) + interval '1 month - 1 day')::date, 'YYYY-MM-DD');

    /* 2. Build params cho eOffice API */
    v_params := jsonb_build_object(
        'date_start', v_date_start,
        'date_end', v_date_end,
        'limit', 1000
    );

    IF p_manv <> '' THEN
        v_params := v_params || jsonb_build_object('user_code', p_manv);
    END IF;

    /* 3. Gọi API eOffice data-internal-sign qua helper get_python_eo_data */
    v_raw_response := public.get_python_eo_data(
        jsonb_build_object(
            'url', 'https://eoffice.meraplion.com/admincp/api/api/raw/data-internal-sign',
            'params', v_params
        )
    );

    /* 4. Kiểm tra phản hồi từ API */
    IF v_raw_response->>'status' = 'error' THEN
        RETURN jsonb_build_object(
            'status', 'fail',
            'error_message', COALESCE(v_raw_response->>'message', 'Lỗi khi gọi eOffice API')
        );
    END IF;

    /* 5. Trích xuất danh sách và chuẩn hóa format JSON trả về theo write_get_function.md */
    RETURN (
        WITH rows_data AS (
            SELECT
                elem->>'form_code' AS form_code,
                elem->>'form_name' AS form_name,
                elem->>'created_at' AS created_at,
                elem->>'updated_at' AS updated_at,
                elem->>'created_code' AS created_code,
                elem->>'created_name' AS created_name,
                elem->>'current_step_status' AS current_step_status,
                elem->>'attachment_name' AS attachment_name,
                elem->>'attachment_url' AS attachment_url
            FROM jsonb_array_elements(
                CASE 
                    WHEN jsonb_typeof(v_raw_response->'data') = 'array' THEN v_raw_response->'data'
                    ELSE '[]'::jsonb
                END
            ) AS elem
            WHERE (p_manv = '' OR elem->>'user_code' = p_manv OR elem->>'created_code' = p_manv)
            ORDER BY elem->>'created_at' DESC, elem->>'id_form' DESC
        )
        SELECT jsonb_build_object(
            'status', 'ok',
            'rows', (SELECT COUNT(*) FROM rows_data),
            'data', COALESCE((SELECT jsonb_agg(f) FROM rows_data f), '[]'::jsonb)
        )
    );

EXCEPTION
    WHEN OTHERS THEN
        RETURN jsonb_build_object(
            'status', 'fail',
            'error_message', SQLERRM
        );
END;
$function$;
```

---

## 3. Kết Quả Kiểm Thử (Unit Test Thực Tế)

Đã deploy và kiểm thử trực tiếp trên database server PostgreSQL (`101.99.42.30:5432`):

1. **Test Case 1: Không truyền bất kỳ param nào (`{}`)**
   - Kết quả: Thành công (`status: ok`), không bị chặn validation, tự động lấy tháng hiện tại.
2. **Test Case 2: Chỉ truyền `ky_chi_phi = '2026-10'` (không truyền `manv`)**
   - Kết quả: Thành công (`status: ok`, `rows: 6`), lấy toàn bộ biểu mẫu ký số trong kỳ của tất cả nhân sự mà không bị chặn lỗi thiếu `manv`.
3. **Test Case 3: Truyền cả `manv = 'MR2366'` và `ky_chi_phi = '2026-10'`**
   - Kết quả: Thành công (`status: ok`, `rows: 3`), lọc chính xác theo nhân viên.

---

## 4. Bổ sung Function `post_python_eo_data`

Dựa trên cấu trúc của [get_python_eo_data.sql](file:///D:/postgres/functions/get_python_eo_data.sql), đã tạo hàm `post_python_eo_data` chuyên dụng để thực hiện HTTP POST request sang eOffice API:
- **Files lưu trữ**:
  - [functions/post_python_eo_data.sql](file:///D:/postgres/functions/post_python_eo_data.sql)
  - [public/functions/post_python_eo_data.sql](file:///D:/postgres/public/functions/post_python_eo_data.sql)
- **Cơ chế hoạt động**:
  - Nhận `payload jsonb` chứa `url`, `params` (nếu có), và dữ liệu gửi POST **chỉ nhận duy nhất qua key `'data'`**. Nếu thiếu key `'data'` hàm sẽ trả về thông báo lỗi `"Missing 'data' parameter in payload"`.
  - Sử dụng chung bộ token & header chuẩn của hệ thống eOffice. Cho phép mở rộng override header nếu cần.
  - Hỗ trợ timeout 15s và tự động xử lý parsing JSON hoặc fallback text phản hồi.
- **Deploy & Test**: Đã deploy thành công lên PostgreSQL server (`101.99.42.30:5432`) và test gửi request thành công.

---

## 5. Bổ sung Function `post_form_claim_chi_phi_signature_form` vào PRD

Đã cập nhật đặc tả chi tiết cho hàm [post_form_claim_chi_phi_signature_form.sql](file:///D:/postgres/public/functions/post_form_claim_chi_phi_signature_form.sql) vào tài liệu PRD [prd_form_claim_chi_phi.md](file:///D:/ai-docs/prd/prd_form_claim_chi_phi.md):
- **Cập nhật Mục 4.6 (User Flow Chốt dữ liệu & Ký số)**: Làm rõ luồng gọi song song giữa API sinh Excel tải về (`post_form_claim_chi_phi_excel`) và API lấy cấu trúc biểu mẫu ký số (`post_form_claim_chi_phi_signature_form`) để lưu vào `signature_form_data`.
- **Cập nhật Mục 6.5 (Đặc tả Function)**: Thêm đầy đủ thông tin:
  - **Mục đích**: Adapter đóng gói dữ liệu từ `get_form_claim_chi_phi_excel_form` sang chuẩn JSON ký số eOffice (`event_name: 'internal_signature_form'`).
  - **Cấu trúc JSON Input & Output**: Bao gồm `file_id`, `bm_tong_hop` (`BMKT002-DNTT`) và mảng `bm_dinh_kem` (`BMKT013-KH-TH-CP`, `BMKT005-DNTTCTP`).

---

## 6. Cập nhật Quy tắc Tìm kiếm Toàn cục vào `C:\Users\Duy Van\.gemini\AGENTS.md`

Đã ghi chú bổ sung quy tắc bắt buộc vào [C:\Users\Duy Van\.gemini\AGENTS.md](file:///C:/Users/Duy%20Van/.gemini/AGENTS.md#L33-L40):
- **Quy tắc Tìm kiếm Codebase & Quét File (Bắt buộc - Tối ưu tốc độ & Tránh đơ/lag)**:
  - Khi tìm kiếm file hoặc quét nội dung (`Get-ChildItem`, `Select-String`, tool search...), bắt buộc luôn loại trừ các thư mục rác/nặng: `node_modules`, `venv`, `.venv`, `.git`, `.next`, `dist`, `build`, `logs`, `.cache`.
  - Không bao giờ quét đệ quy (`-Recurse`) vào thư mục gốc mà không có bộ lọc `Where-Object` loại trừ.
  - Ưu tiên khoanh vùng tìm kiếm trong các thư mục con cụ thể (`src/`, `api/`, `functions/`) thay vì quét toàn bộ repository lớn.

---

## 7. Cập nhật `post_form_claim_chi_phi_signature_form`: Loại bỏ biểu mẫu BMKT005 khi CTP = 0

### Bối cảnh & Vấn đề:
Trước đây, hàm `post_form_claim_chi_phi_signature_form` cố định đưa cả `BMKT013-KH-TH-CP` và `BMKT005-DNTTCTP` vào mảng `bm_dinh_kem`. Khi nhân sự không đi công tác trong tháng (`tong_cong_tac_phi = 0` và không có dòng chi tiết CTP), hệ thống vẫn đóng gói biểu mẫu `BMKT005-DNTTCTP` 0 đồng gửi sang eOffice. Điều này làm phát sinh 1 biểu mẫu rác bắt buộc người dùng và cấp quản lý phải thực hiện thêm thao tác ký duyệt không cần thiết.

### Giải pháp & Thay đổi đã thực hiện:
1. **Sửa mã nguồn Stored Function**:
   - Files:
     - [functions/post_form_claim_chi_phi_signature_form.sql](file:///D:/postgres/functions/post_form_claim_chi_phi_signature_form.sql)
     - [public/functions/post_form_claim_chi_phi_signature_form.sql](file:///D:/postgres/public/functions/post_form_claim_chi_phi_signature_form.sql)
   - Khởi tạo mảng `bm_dinh_kem` ban đầu chỉ có `BMKT013-KH-TH-CP`.
   - Bổ sung điều kiện kiểm tra công tác phí:
     ```sql
     IF COALESCE(v_jsonb_from_old_function->>'bmkt005_tong_cong_tac_phi', '0') NOT IN ('0', '')
        AND COALESCE(jsonb_array_length(v_jsonb_from_old_function->'BMKT005'), 0) > 0 THEN
         v_bm_dinh_kem := v_bm_dinh_kem || jsonb_build_array(
             jsonb_build_object(
                 'ma_bieu_mau', 'BMKT005-DNTTCTP',
                 'ten_bieu_mau', 'BẢNG KÊ CHI TIẾT CÔNG TÁC PHÍ THÁNG ' || v_month_year,
                 'ma_nv', v_jsonb_from_old_function->>'bmkt002_ma_nv',
                 'ten_nv', v_ten_nv,
                 'tong_tien', v_jsonb_from_old_function->>'bmkt005_tong_cong_tac_phi',
                 'ly_do_thanh_toan', v_jsonb_from_old_function->>'bmkt005_ly_do_thanh_toan',
                 'so_tien_bang_chu', v_jsonb_from_old_function->>'bmkt005_so_tien_bang_chu',
                 'ds_chi_tiet', v_jsonb_from_old_function->'BMKT005'
             )
         );
     END IF;
     ```
2. **Deploy lên Database**:
   - Đã thực thi deploy cập nhật trực tiếp lên database PostgreSQL (`101.99.42.30:5432`, db: `postgres`).
3. **Kiểm thử thực tế (Unit Test)**:
   - Chạy test với nhân sự `MR2366` kỳ `2026-10` (nhân viên không có CTP):
     - Kết quả: `len(bm_dinh_kem) = 1` chỉ có duy nhất `BMKT013-KH-TH-CP`. Biểu mẫu `BMKT005-DNTTCTP` đã hoàn toàn được loại bỏ, không còn phát sinh biểu mẫu 0 đồng.
4. **Cập nhật tài liệu PRD**:
   - Đã cập nhật mục `post_form_claim_chi_phi_signature_form` trong [prd_form_claim_chi_phi.md](file:///D:/ai-docs/prd/prd_form_claim_chi_phi.md) với quy tắc loại trừ và 2 trường hợp JSON Output mẫu (Trường hợp 1: Không có CTP; Trường hợp 2: Có CTP).

---

## 8. Tải dữ liệu được duyệt & Ký số: Bổ sung Trạng thái Ký số và Khóa gửi ký (`allow_send_data_internal_sign`)

### Yêu cầu người dùng:
1. Đổi text nút "Tải dữ liệu đã được duyệt" trên màn hình Claimed thành **"Tải dữ liệu được duyệt và ký số"**.
2. Gọi thêm API `get_form_claim_chi_phi_forward_data_internal_sign` để hiển thị trực quan biểu mẫu nào đã ký hoặc chưa ký trong kỳ chi phí được chọn.
3. Backend bổ sung key `allow_send_data_internal_sign (1|0)`:
   - Giá trị `1` khi **cả 3 mẫu đều chưa tồn tại** HOẶC **tất cả mẫu đều ở trạng thái (reject|cancel)**.
   - Giá trị `0` khi có ít nhất 1 mẫu đang xử lý (`pending`, `approved`, `signed`...).
4. Frontend dựa vào `allow_send_data_internal_sign` để disable hoặc mở nút gửi ký ("✍️ Tôi đã xem và tiến hành ký số thông qua MLID").

### Chi tiết triển khai:

#### 1. Backend PostgreSQL:
- **Files cập nhật**:
  - [public/functions/get_form_claim_chi_phi_forward_data_internal_sign.sql](file:///D:/postgres/public/functions/get_form_claim_chi_phi_forward_data_internal_sign.sql)
  - [functions/get_form_claim_chi_phi_forward_data_internal_sign.sql](file:///D:/postgres/functions/get_form_claim_chi_phi_forward_data_internal_sign.sql)
- **Logic bổ sung**:
  - Trả về thêm `is_signed` (0|1) và `status_signed` ("Chưa ký" | "Đã ký") cho từng dòng biểu mẫu.
  - Tính toán `allow_send_data_internal_sign` cấp Root:
    ```sql
    latest_forms AS (
        SELECT DISTINCT ON (form_code)
            form_code,
            current_step_status,
            created_at,
            id_form
        FROM raw_rows
        WHERE form_code IN ('BMKT002-DNTT', 'BMKT013-KH-TH-CP', 'BMKT005-DNTTCTP')
        ORDER BY form_code, created_at DESC, id_form DESC
    ),
    calc_allow AS (
        SELECT
            CASE
                WHEN (SELECT COUNT(*) FROM latest_forms) = 0 THEN 1
                WHEN NOT EXISTS (
                    SELECT 1 FROM latest_forms
                    WHERE LOWER(COALESCE(current_step_status, '')) NOT IN ('reject', 'cancel', 'rejected', 'cancelled')
                ) THEN 1
                ELSE 0
            END AS allow_send
    )
    ```
- **Deploy**: Đã deploy thành công lên DB server PostgreSQL (`101.99.42.30:5432`). Đã test kiểm tra HTTP GET qua `/local/get_data/get_form_claim_chi_phi_forward_data_internal_sign/`.

#### 2. Frontend React:
- **File cập nhật**: [form_claim_chi_phi_claimed.jsx](file:///D:/django_apps/rest/frontend1/src/formcontrol/form_claim_chi_phi/form_claim_chi_phi_claimed.jsx)
- **Các thay đổi & Rationale (Lý do thiết kế)**:
  1. **Đổi text nút chính**: Đổi thành `"Tải dữ liệu được duyệt và ký số"`. Khi click mở Modal, chỉ khởi tạo form và reset state, **không tự động gọi API ngầm**.
  2. **Dropdown chọn tháng (`handle_month_change`)**: Khi đổi tháng, chỉ cập nhật kỳ chi phí và xóa các dữ liệu/kết quả cũ (`internal_sign_data = null`). **Không tự động gọi API khi vừa đổi dropdown** nhằm tránh spam request không cần thiết khi người dùng đang lướt chọn tháng.
  3. **Trigger API qua nút "🚀 Tải data" (`handle_confirm`)**: Khi người dùng ấn nút "🚀 Tải data", hệ thống gọi đồng thời qua `Promise.all` cả 3 API:
     - `post_form_claim_chi_phi_excel`: Sinh file excel tải về.
     - `post_form_claim_chi_phi_signature_form`: Lấy cấu trúc payload ký số.
     - `get_form_claim_chi_phi_forward_data_internal_sign`: Lấy tiến độ ký số và cờ `allow_send_data_internal_sign` từ eOffice.
  4. **Giữ nguyên trạng thái ký số từ eOffice**:
     - Hiển thị trực tiếp giá trị chuỗi gốc `item.status_signed` (`"Đã ký"`, `"Chưa ký"`) và `item.current_step_status` (`pending`, `reject`...) từ eOffice trả về, không tự ý ánh xạ hay sửa đổi tên trạng thái.
     - Kèm link tải file ký số hoàn tất `item.attachment_url` nếu có.
  5. **Kiểm soát nút gửi ký**: Nút `"✍️ Tôi đã xem và tiến hành ký số thông qua MLID"` chỉ hiển thị sau khi đã tải data, và sẽ tự động `disabled` nếu `internal_sign_data.allow_send_data_internal_sign === 0` kèm thông báo cảnh báo.
  6. **Tối ưu hóa giao diện Modal trên Mobile**:
     - Thêm `centered` giúp Modal luôn căn giữa màn hình điện thoại, không dính mép trên.
     - Dùng cấu trúc `flex-grow-1` (trái) và `flex-shrink-0` (phải) cho từng dòng biểu mẫu, đảm bảo huy hiệu trạng thái (`Đã ký` / `Chưa ký`) và link tải file ký không bao giờ bị chèn ép, co méo hay vỡ dòng trên màn hình nhỏ (360px - 400px).
     - Nút *"Tải xuống dữ liệu Excel"* chuyển thành full-width (`w-100 py-2`), dễ chạm ngón tay cái.
     - Nút *"✍️ Tôi đã xem và tiến hành ký số qua MLID"* điều chỉnh cỡ chữ `0.95rem` và `lineHeight 1.4` tránh bị tràn thành 3-4 dòng xấu trên mobile.
     - Footer Modal phân chia đều `flex-fill` (50/50) cho 2 nút *"✖️ Đóng"* và *"🚀 Tải data"*.

#### 3. Tài liệu PRD:
- Cập nhật đặc tả API `get_form_claim_chi_phi_forward_data_internal_sign` trong [prd_form_claim_chi_phi.md](file:///D:/ai-docs/prd/prd_form_claim_chi_phi.md#L1790-L1880).

---

## 9. Triển khai Deploy Frontend Web & Bổ sung Task Kế Hoạch CTP

### 1. Deploy Frontend Web Thành Công (`deploy.ps1`):
- Đã thực thi hoàn tất script [deploy.ps1](file:///D:/django_apps/rest/frontend1/deploy.ps1):
  1. Cập nhật `OTA_BUILD_TIME` thành `23/09/2026 11:35:31` trong [string.js](file:///D:/django_apps/rest/frontend1/src/utils/string.js).
  2. Build production React (`react-app-rewired build`) thành công (size gzip: `main.js 825.54 kB`).
  3. Deploy lên **Firebase Hosting** (`spatial-vision-343005`) hoàn tất.
  4. Đồng bộ SCP toàn bộ build files sang remote server `reactbi@118.69.42.141:/var/www/react_app/build`.
  5. Đồng bộ thư mục `formcontrol` từ `frontend1` sang `testbi`.

### 2. Bổ sung Task vào Kế hoạch PRD (`ke_hoach_cap_nhat_prd_claim_chi_phi.md`):
- Đã ghi nhận task mới **BE-2.6** tại [ke_hoach_cap_nhat_prd_claim_chi_phi.md](file:///D:/ai-docs/ke_hoach_cap_nhat_prd_claim_chi_phi.md#L170-L184):
  - **Nhiệm vụ**: Cập nhật Stored Function `get_form_claim_chi_phi_excel_form` để **ưu tiên tiền vé xe từ hóa đơn (`form_claim_chi_phi_hoa_don.so_tien_claim` với `cost_type = 'transport'`)** thay vì chỉ lấy số tiền đề xuất ban đầu từ `form_cong_tac_phi`.
  - **Công thức áp dụng**:
    ```sql
    COALESCE(
        NULLIF((SELECT SUM(so_tien_claim) FROM public.form_claim_chi_phi_hoa_don WHERE khid = cp.khid AND cost_type = 'transport'), 0),
        COALESCE(cp.ve_xe_cong_tac, 0) + COALESCE(cp.tong_tien_ve_xe, 0)
    ) AS ve_xe
    ```

---

## 10. Tinh chỉnh UI Trạng thái Ký số & Màu chữ cảnh báo khóa nút gửi

### 1. Đồng nhất 01 Badge trạng thái duy nhất (Bỏ hiển thị `(pending)` gây hiểu nhầm):
- **File**: [form_claim_chi_phi_claimed.jsx](file:///D:/django_apps/rest/frontend1/src/formcontrol/form_claim_chi_phi/form_claim_chi_phi_claimed.jsx)
- **Vấn đề**: Trước đó, biểu mẫu vừa hiện badge `[Đã ký]` vừa hiện dòng chữ phụ `(pending)` bên dưới (do eOffice trả về `status_signed = "Đã ký"` của cá nhân nhân viên, nhưng `current_step_status = "pending"` cho tiến trình tài liệu chung của các cấp ký tiếp theo), gây hiểu lầm là chưa ký.
- **Giải pháp**:
  - Loại bỏ hoàn toàn dòng text phụ `{item.current_step_status}`.
  - Hiển thị duy nhất 01 Badge trạng thái rõ ràng:
    - `Đã ký` (Badge xanh `bg="success"`) nếu `item.status_signed === "Đã ký"`. Kèm link `📄 Tải file ký` nếu có `attachment_url`.
    - `Từ chối` (Badge đỏ `bg="danger"`) nếu `current_step_status` thuộc nhóm từ chối/hủy (`reject`, `cancel`, `rejected`, `cancelled`).
    - `Chưa ký` (Badge vàng `bg="warning"`) cho các trường hợp còn lại.

### 2. Cập nhật màu chữ cảnh báo khóa nút gửi ký:
- Đổi class thông báo từ `text-warning` sang `text-danger` (màu đỏ) nổi bật, dễ đọc:
  `⚠️ Hồ sơ ký số kỳ này đang trong tiến trình ký/duyệt (hoặc đã ký duyệt). Nút gửi ký đang được khóa để tránh gửi trùng lặp.`

### 3. Deploy Frontend Web Thành Công:
- Đã thực thi script [deploy.ps1](file:///D:/django_apps/rest/frontend1/deploy.ps1) (Build time: `23/09/2026 11:45:10`).
- Build production hoàn tất, deploy Firebase Hosting và SCP sang remote server `118.69.42.141` thành công 100%.

