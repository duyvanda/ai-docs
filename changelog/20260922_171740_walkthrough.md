# Walkthrough: Tái Cấu Trúc Kế Hoạch PRD Claim Chi Phí Thành TODO List Chuẩn (Backend Trước, Frontend Sau)

## Thông tin tổng quan
- **Thời gian thực hiện**: 22/09/2026 17:17:40 (Giờ địa phương)
- **Tài liệu duy nhất được cập nhật**: [ke_hoach_cap_nhat_prd_claim_chi_phi.md](file:///D:/ai-docs/ke_hoach_cap_nhat_prd_claim_chi_phi.md)
- **Yêu cầu của User**: Không tách thành 2 file riêng biệt mà cấu trúc trực tiếp file kế hoạch gốc thành một danh sách **TODO List** chi tiết, phân rã theo thứ tự: **Backend trước, Frontend sau**.
- **Tình trạng file tạm**: Đã xóa các file tách tạm thời, giữ cho repository sạch sẽ.

---

## 1. Cấu trúc mới của file [ke_hoach_cap_nhat_prd_claim_chi_phi.md](file:///D:/ai-docs/ke_hoach_cap_nhat_prd_claim_chi_phi.md)

Tài liệu được chuẩn hóa thành 4 phần chính, trong đó các nhiệm vụ kỹ thuật được phân rã thành checklist `- [ ]` có mã định danh rõ ràng (`BE-x.x` và `FE-x.x`):

### I. Nguyên Tắc Nghiệp Vụ Cốt Lõi (Core Business Rules)
- Tóm lược chuỗi trạng thái: `H -> C -> I -> D -> DKT`, `RKT`, `R`, `E`.
- Khẳng định quy tắc: **Người gánh chi phí (`manv_quan_ly_chi_phi`) duyệt xuyên suốt cả 2 lần (Lần 1: Kế hoạch; Lần 2: Hóa đơn)**.
- Quy tắc ngoại lệ 03 nhân sự cấp cao tự làm - tự duyệt (`MR1137`, `MR0485`, `MR2685`).
- Quyền hạn Kế toán viên: Điều chỉnh `so_tien_claim` ($0 < \text{so\_tien\_claim} \le \text{tong\_tien\_hoa\_don}$), loại bỏ HĐ (thực hiện `DELETE` trực tiếp để giải phóng HĐ tự do). Khi từ chối `RKT` thì phiếu đóng vĩnh viễn, nhân viên không thể làm lại trên phiếu này.
- Quy tắc tách dòng thuế HĐ Misa & trừ chênh lệch vào dòng có số tiền lớn nhất (`rn_max = 1`).

### II. PHẦN 1: TODO LIST KỸ THUẬT - BACKEND (BE)
- **BE-1: Database Schema & DDL**:
  - `BE-1.1`: DDL `ALTER TABLE form_claim_chi_phi` thêm 4 cột (`manv_quan_ly_chi_phi`, `kt_approved_manv`, `kt_approved_at`, `kt_note`).
  - `BE-1.2`: Xác nhận cơ chế bảng HĐ (không thêm cột, `DELETE` trực tiếp khi loại bỏ hoặc từ chối `RKT`).
  - `BE-1.3`: Cập nhật View `view_form_claim_chi_phi_ge_bang_ke` chuẩn 42 cột (thêm `mau_so`, `seri`, `thue_suat`, `hinh_thuc_thanh_toan`, `ten_ke_toan_phu_trach`, lọc `WHERE a.status = 'DKT'`).
- **BE-2: Cập Nhật Stored Functions Hiện Hữu**:
  - `BE-2.1`: `get_form_claim_chi_phi` trả về `lst_quan_ly_chi_phi` (TDV: 3 options, CRM: 2 options, ASM: 1 option, NCRM: `[]`).
  - `BE-2.2`: `insert_form_claim_chi_phi` nhận `manv_quan_ly_chi_phi` và kiểm tra hạn mức ngân sách theo người gánh CP.
  - `BE-2.3`: `get_form_claim_chi_phi_crm` (Lần 1) lọc theo `a.manv_quan_ly_chi_phi = p_manv` (kèm ngoại lệ 3 nhân sự).
  - `BE-2.4`: `get_form_claim_chi_phi_crm_claimed` (Lần 2) đồng bộ lọc theo `a.manv_quan_ly_chi_phi = p_manv` (kèm ngoại lệ 3 nhân sự).
  - `BE-2.5`: `insert_form_claim_chi_phi_crm_claimed` duyệt ghi `status = 'D'` hoặc từ chối ghi `status = 'E'`.
- **BE-3: Xây Dựng 02 Stored Functions & Endpoints Mới Cho Kế Toán**:
  - `BE-3.1`: GET `get_form_claim_chi_phi_kt_review` (URL, query params, output JSON Tiếng Việt snake_case kèm mảng lồng `lst_hoa_don`).
  - `BE-3.2`: POST `insert_form_claim_chi_phi_kt_claimed` (nhận JSON array, xử lý duyệt chi `DKT`, cập nhật `so_tien_claim`, xóa HĐ bị loại, tính lại tổng tiền claim của phiếu, hoặc xử lý từ chối `RKT` nhả toàn bộ HĐ và đóng vĩnh viễn).
- **BE-4: Migration & Kiểm Thử Backend**:
  - `BE-4.1`: Script migration dữ liệu cũ: `UPDATE form_claim_chi_phi SET status = 'DKT' WHERE status = 'D';`.
  - `BE-4.2`: Checklist Unit Test các SQL Function.
  - `BE-4.3`: Đăng ký URL routing trên Django REST Gateway và deploy server.

### III. PHẦN 2: TODO LIST KỸ THUẬT - FRONTEND (FE)
- **FE-1: Phân Hệ Tạo Kế Hoạch Chi Phí (`FE Web` & `FE App`)**:
  - `FE-1.1`: Tích hợp API đọc `lst_quan_ly_chi_phi`.
  - `FE-1.2`: Thêm component Dropdown / Radio Group "Đối tượng ghi nhận chi phí" (tự chọn option `mac_dinh = true`).
  - `FE-1.3`: Gửi `manv_quan_ly_chi_phi` lên API `insert_form_claim_chi_phi`.
- **FE-2: Phân Hệ Quản Lý Duyệt Kế Hoạch & Hóa Đơn (`FE Web` & `FE App`)**:
  - `FE-2.1`: Phân quyền khóa nút tự duyệt (chỉ mở cho 3 nhân sự đặc quyền).
  - `FE-2.2`: Bổ sung Badge `[Phiếu phân bổ chi phí cho bạn]`.
- **FE-3: Phân Hệ MỚI Màn Hình Kế Toán Duyệt Chi (`FE Web`)**:
  - `FE-3.1`: Cấu hình route `/formcontrol/form_claim_chi_phi_kt_review` và menu Kế toán.
  - `FE-3.2`: Thanh Filter (Kỳ chi phí, Kênh, Tìm kiếm nhanh).
  - `FE-3.3`: Data table danh sách phiếu `status = 'D'` kèm thanh Bar tổng số phiếu và tổng tiền.
  - `FE-3.4`: Drawer chi tiết hồ sơ:
    - Danh sách HĐ (mẫu số, seri, MST, thuế suất, link PDF, link chứng từ).
    - Ô sửa trực tiếp `so_tien_claim` (realtime tính lại tổng tiền claim phiếu).
    - Nút chọn `[Hợp lệ]` / `[Loại bỏ]` kèm ô nhập lý do loại bỏ.
    - Ô ghi chú Kế toán `kt_note`.
    - Nút "Xác nhận Duyệt chi" (`status = 'DKT'`) và "Từ chối hồ sơ" (`status = 'RKT'` kèm Modal cảnh báo phiếu bị hủy vĩnh viễn).
- **FE-4: Phân Hệ Báo Cáo Bảng Kê GE (`FE Web`)**:
  - `FE-4.1`: Bổ sung 5 cột mới trên Data Table Báo cáo Bảng kê GE.
  - `FE-4.2`: Cập nhật chức năng Xuất Excel đủ 42 cột đồng bộ.
- **FE-5: Kiểm Thử UX/UI & Liên Thông**:
  - Checklist test từng màn hình, format tiền tệ VNĐ và format ngày tháng.

### IV. Kế Hoạch Phối Hợp & Tiến Độ Triển Khai (Rollout Timeline)
- Lộ trình 3 giai đoạn từ 23/09/2026 đến hạn chót Go-Live ngày 28/09/2026.

---

## 2. Danh mục tài liệu tác động
- **File cập nhật duy nhất**: [ke_hoach_cap_nhat_prd_claim_chi_phi.md](file:///D:/ai-docs/ke_hoach_cap_nhat_prd_claim_chi_phi.md)
- **Hiệu chỉnh bổ sung**: 
  1. Đã loại bỏ ràng buộc mô tả thứ tự chuỗi cột hóa đơn (`... ten_nha_cung_cap -> mst_ncc -> ...`) ở cả 2 mục BE-1.3 và FE-4.1 theo yêu cầu của User (chỉ cần bổ sung đủ các trường/cột mới là đạt yêu cầu).
  2. Làm rõ toàn diện logic tính toán `rn_max = 1` tại [Mục I.5](#quy-tắc-tách-dòng-thuế-hđ-misa--điều-chỉnh-số-tiền-logic-rn_max--1) và [Mục BE-1.3](#be-13-cập-nhật-ddl-view-view_form_claim_chi_phi_ge_bang_ke-chuẩn-42-cột) của tài liệu: giải thích rõ nguyên nhân tách dòng theo thuế suất Misa, lý do giảm trừ chỉ trừ vào dòng có số tiền lớn nhất (tránh phân bổ lẻ tiền thuế), công thức chi tiết và kèm bảng số liệu ví dụ số học đối soát trước/sau điều chỉnh.
  3. Bổ sung đặc tả chi tiết và tường minh cấu trúc **JSON Input** và **JSON Output** cho cả 3 mục: **BE-2.1** (`get_form_claim_chi_phi`), **BE-3.1** (`get_form_claim_chi_phi_kt_review`), và **BE-3.2** (`insert_form_claim_chi_phi_kt_claimed` gồm cả trường hợp `DKT`, `RKT`, response thành công và lỗi).
  4. Thu gọn JSON Output của **BE-2.1**: Chỉ hiển thị duy nhất key mới cần bổ sung (`lst_quan_ly_chi_phi`) để tài liệu ngắn gọn, tập trung đúng phần thay đổi theo yêu cầu.
  5. Loại bỏ tham số **Kênh** (`kenh`) khỏi API Kế toán duyệt chi ở cả 2 phía: **BE-3.1** (bỏ khỏi URL Gateway, Query Params và JSON Input của `get_form_claim_chi_phi_kt_review`) và **FE-3.2** (bỏ dropdown lọc kênh trên Filter bar).
  6. Loại bỏ mục **BE-4.3** ("Cấu hình Django REST Gateway & Deploy Server"): Các endpoint trong Django REST Gateway của hệ thống được cấu hình tự động map theo tên Stored Function PostgreSQL (`/get_data/<ten_ham>/` và `/post_data/<ten_ham>/`), không cần khai báo route thủ công và chỉ deploy khi có lệnh trực tiếp.
  7. Tinh gọn toàn diện **Phần I (Nguyên tắc nghiệp vụ cốt lõi)**: Viết dạng bullet notes ngắn gọn, súc tích, lược bỏ các câu văn dài dòng nhưng vẫn giữ trọn vẹn 100% logic nghiệp vụ (chuỗi trạng thái, người gánh CP duyệt xuyên suốt, 3 nhân sự ngoại lệ, quyền sửa tiền/hủy phiếu của KT, logic và công thức `rn_max = 1` cùng ví dụ số học minh họa).


