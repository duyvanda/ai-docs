# Walkthrough: Đọc và phân tích File Google Sheets Dự án Merap - Medigo

## Thông tin tổng quan
- **Thời gian thực hiện**: 15/09/2026 13:35:00 (Giờ địa phương)
- **File Google Sheets**: `https://docs.google.com/spreadsheets/d/16fbt1EXWb7-pE8rBPDQzJtYu4DwWGP076QxZvFccluU`
- **Tiêu đề tài liệu**: **Project timeline Merap-Medigo**
- **Sheet trọng tâm**: `Danh sách NT đăng ký` (gid: `1531697996`)

---

## 1. Cấu trúc tổng thể của Workbook
Tài liệu gồm 4 tab (sheets):
1. **Danh sách NT đăng ký** (`gid=1531697996`): Bảng danh sách 127 nhà thuốc đăng ký tham gia dịch vụ tư vấn Eclinic của Medigo.
2. **Quy trình Triển khai** (`gid=1901496096`): Gồm 7 bước từ B0 đến B6 (Ký HĐ, Đặt cọc 50%, Gửi TLHD, Lập danh sách, Tạo tài khoản, TDV hướng dẫn NT, Medigo đào tạo sử dụng, Hoàn tất & theo dõi).
3. **Danh mục sp ưu tiên** (`gid=1240232631`): Danh mục sản phẩm hiển thị ưu tiên trên App Bác sĩ (hiện đang là khung mẫu, chưa điền tên sản phẩm).
4. **Lịch offline** (`gid=1185985210`): Lịch hướng dẫn trực tiếp cho nhà thuốc (hiện đang là khung mẫu).

---

## 2. Phân tích chi tiết Sheet "Danh sách NT đăng ký"

### 2.1. Số liệu cốt lõi
- **Tổng số Nhà thuốc**: **127 Nhà thuốc** (toàn bộ 100% là khách hàng `New` đợt 01/09/2026).
- **Tổng số lượt tư vấn cài đặt**: **3,000 lượt**.
- **Phân bổ theo Chi nhánh**:
  - **HYN017** (Miền Bắc): **73 NT** | **1,855 lượt** (~61.8%)
  - **HCM001** (Miền Nam & ĐBSCL, Tây Nguyên): **48 NT** | **1,010 lượt** (~33.7%)
  - **DNG013** (Miền Trung): **6 NT** | **135 lượt** (~4.5%)
- **Phân bổ theo Gói số lượt tư vấn**:
  - Gói 10 lượt: 57 NT
  - Gói 20 lượt: 28 NT
  - Gói 50 lượt: 16 NT
  - Gói 30 lượt: 15 NT
  - Gói 15 lượt: 3 NT
  - Gói 60 lượt: 3 NT (NT Thắng Hương, NT Hà Dương, NT Duy Anh 2)
  - Gói 75 lượt: 3 NT (CT ABC Pharmacare An Hải, NT Nhân Dân 2 Bỉm Sơn, CT Kiều Khải Hoàn)
  - Gói 70 lượt: 1 NT (AMG Pharma Mao Điền)
  - Gói 100 lượt: 1 NT (Ngọc Đông Bắc Giang - lớn nhất)

### 2.2. Tiến độ triển khai hệ thống (Đối chiếu Quy trình)
- Đang ở giai đoạn giao thoa giữa **Bước 3** và **Bước 4**:
  - **Phía MEDIGO (Bước 3 - Cấp tài khoản Eclinic)**: Đã tạo và cấu hình xong **125/127 NT** (`Done Set Up` = 'x'). Tên đăng nhập là SĐT của nhà thuốc, mật khẩu mặc định: `123456`, email: `merap@gmail.com`.
  - **Phía MERAP (Bước 4 - TDV gửi tài khoản & hướng dẫn NT đăng nhập)**: Cột `NT nhận TLHD và tài khoản từ TDV` hiện vẫn để trống 100% (chưa có ghi nhận bàn giao thực tế).
  - **Bước 5 & 6 (Sử dụng lượt tư vấn & Báo cáo)**: Cột `Used`, `Kết quả hướng dẫn`, `Next Action`, `Phản hồi của TDV MERAP` hoàn toàn trống.

---

## 3. Các vấn đề dữ liệu cần lưu ý & xử lý ngay
1. **2 Nhà thuốc chưa thể tạo tài khoản do thiếu SĐT**:
   - `STT 1` (Mã KH: `019244`): CT TNHH ABC PHARMACARE - An Hải - Đà Nẵng (CRM: Huỳnh Văn Huy, TDV: Nguyễn Thị Khánh Vy). Đăng ký gói lớn: 75 lượt.
   - `STT 10` (Mã KH: `N02208228`): NT Hậu Nghĩa - Trảng Bàng - Tây Ninh (CRM: Lê Đức Châu, TDV: Nguyễn Duy Anh).
2. **1 Nhà thuốc thiếu SĐT ở phía Merap nhưng Medigo đã tự điền SĐT tạo tài khoản**:
   - `STT 35` (Mã KH: `014585`): NT An Đức 4 - Thanh Hóa (Medigo tạo tài khoản với SĐT `0982414926`). Cần kiểm tra lại tính chính xác của SĐT này.
3. **1 Nhà thuốc bị lỗi tên TDV**:
   - `STT 46` (Mã KH: `TD31O299`): NT Mai Hương - Kim Thành - Hải Dương (Tên TDV bị `#N/A`, SĐT TDV: `0912750694`).
4. **1 Nhà thuốc ghi thiếu thông tin giấy phép GPP**:
   - `STT 91` (Mã KH: `022325`): CTy TNHH MTV TM DV DP Kiều Khải Hoàn - Vĩnh Long (Số GPP chỉ ghi `/GPP`).
5. **4 Nhà thuốc chưa điền Mật khẩu & Email trên sheet**:
   - `STT 2, 3, 4, 6`: Đã có Tên đăng nhập và tích `x` nhưng ô Mật khẩu và Email bị bỏ trống.
6. **Lỗi công thức tính % trên Sheet**:
   - Dòng 13 có các ô công thức bị lỗi `#VALUE!` ở cột `% Set Up` và `#DIV/0!` ở cột `% Used`.

---

## 4. Đọc & Phân tích Tài liệu PRD Mẫu (`tracking_chi_phi_tp_vttd_m_session.md`)
- **Đường dẫn**: `D:\ai-docs\prd\tracking_chi_phi_tp_vttd_m_session.md`
- **Tên module**: Module Quản lý M.Session – Hội thảo Y khoa TP.
- **Mục đích**: Số hóa toàn diện vòng đời tổ chức hội thảo y khoa tại Nhà thuốc hoặc bên ngoài:
  - Lập đề xuất sự kiện, chọn NT, nhân sự tham gia, chọn nhãn tập trung (tối đa 4), ngày giờ ca, địa điểm, chi phí.
  - Phân công & duyệt sự kiện bởi CXS/CXM (kiểm tra trùng lịch, chọn vật tư VTTD/quà tặng mang theo).
  - CRM/CRS xác nhận tham dự trước sự kiện (`I`) hoặc hủy (`X`).
  - Nộp chứng từ sau sự kiện: Upload file zip hóa đơn PDF và ảnh hiện trường qua `multipart/form-data`, quyết toán chi phí thực tế.
- **Kiến trúc kỹ thuật chuẩn Merap**:
  - **Database**: PostgreSQL Stored Functions nhận/trả `JSONB` hoặc `multipart/form-data`.
  - **Gateway**: `https://bi.meraplion.com/local/get_data/<ten_ham>` và `/post_data/<ten_ham>`.
  - **Bảng chính**: `tracking_chi_phi_tp_m_session`, `tracking_chi_phi_tp_m_session_danh_sach_khach_hang_file_roi`, bảng cấu hình `settings_data`.
  - **Tích hợp bên ngoài**: Bảng nhân sự (`d_hr_dsns`, `d_users`), tuyến bán hàng (`api_f_thongtin_tuyen_mcp_tp_pcl`), khách hàng (`d_master_khachhang`), danh sách Zalo OA (`f_crawl_activate_ecom`), và doanh số lịch sử (`f_raw_data_sales_yoy`).

---

## 5. Tạo PRD & API Hoàn Chỉnh: Module Đăng Ký Tư Vấn Medigo Eclinic
- **Đường dẫn file**: `D:\ai-docs\prd\tracking_chi_phi_tp_medigo_dang_ky.md`
- **Tên module**: Module Đăng ký Tư vấn Eclinic Medigo – Kênh TP.
- **Nội dung hoàn thành**:
  1. **User Flow & Vòng đời**:
     - Tự động nạp danh sách tháng cũ (`01/09/2026`) sang tháng mới (`01/10/2026`) với trạng thái `Renew`.
     - Cho phép CRM sửa thông tin, xem `% Used` tháng cũ và 2 con số doanh số (Doanh số tháng cũ Có VAT & Doanh số YTD Có VAT).
     - Cho phép xóa NT cũ chuyển sang cờ `Stop` (ngưng tham gia).
     - Cho phép thêm mới NT trong tuyến bán hàng của CRM (`New`) với các trường bắt buộc (Mã KH, Tên NT, Thời gian mở cửa, SĐT NT, Tên đại diện NT, SĐT TDV, Số lượt tư vấn cài đặt, Ghi chú; bỏ qua GPP).
     - Quy trình duyệt 2 bước (Không có trạng thái Lưu nháp): CRM chỉnh sửa trực tiếp trên form được nạp từ tháng cũ, sau đó bấm nút duy nhất "Chốt danh sách" (`PENDING`) $\rightarrow$ Quản lý cấp trên (GĐBH/Admin) duyệt (`APPROVED`) hoặc từ chối (`REJECTED` mở lại form cho CRM sửa và chốt lại).
  2. **Database Schema & Chuẩn hóa theo `tracking_chi_phi_tp_vttd_m_session.md`**:
     - Bỏ bảng riêng `tracking_chi_phi_tp_medigo_usage` và bỏ khối code raw DDL SQL.
     - Gom toàn bộ các cột đối soát (`so_luot_dang_ky`, `so_luot_su_dung`, `phan_tram_used`) trực tiếp vào bảng chính duy nhất: `tracking_chi_phi_tp_medigo_dang_ky`.
     - Chuẩn hóa format bảng 3 cột (`Column Name`, `Data Type`, `Description`) và phân vùng hệ thống `| **--- SYSTEM ---** | | |`.
     - Bổ sung trường `manv` (Mã CRM lập danh sách) và `rejected_at` (Thời gian từ chối).
     - Chuẩn hóa mã trạng thái theo chuẩn Merap: `H` (Chờ duyệt / Pending), `C` (Đã duyệt / Approved), `R` (Từ chối / Rejected).
     - View báo cáo & xuất Medigo `v_tracking_chi_phi_tp_medigo_dang_ky_bao_cao`: Lấy trực tiếp dữ liệu đăng ký & đối soát từ bảng chính, mapping thêm thông tin khách hàng, chi nhánh, CRM, TDV, GPP.
  3. **Tái cấu trúc API tinh gọn theo luồng UX Frontend**:
      - `get_tracking_chi_phi_tp_medigo_danh_sach` (API 1 - Gộp): Khởi tạo form và lấy danh sách NT tháng mới (kế thừa tháng cũ nếu chưa chốt/lưu, kèm đầy đủ % Used và Doanh số tháng cũ/YTD). Loại bỏ các trường giả định dư thừa ở root (`status_chot`, `ly_do_tu_choi`), đưa về đúng bản chất dữ liệu theo từng dòng NT trong `data`.
      - `get_tracking_chi_phi_tp_medigo_tuyen_nt` (API 2 - Tuyến NT): API chuyên biệt gọi khi CRM bấm "+ Thêm Nhà thuốc mới", trả về `nt_options` (danh sách NT trong tuyến kèm cờ `da_co_trong_danh_sach` và doanh số tham khảo).
      - `insert_tracking_chi_phi_tp_medigo_chot_danh_sach` (API 3): CRM bấm "Chốt danh sách" gửi toàn bộ NT (New, Renew, Stop) với `status = 'H'`. Loại bỏ `status_chot` ở response output.
      - `insert_tracking_chi_phi_tp_medigo_phe_duyet` (API 4): Quản lý duyệt (`status = 'C'`) hoặc từ chối (`status = 'R'` kèm `ly_do_tu_choi`). Loại bỏ `status_chot` ở response output.
      - `insert_tracking_chi_phi_tp_medigo_sync_usage` (API 5): Nhận dữ liệu số lượt used từ Medigo cập nhật trực tiếp vào bảng chính `tracking_chi_phi_tp_medigo_dang_ky`.
      - `get_tracking_chi_phi_tp_medigo_export` (API 6): Xuất dữ liệu bàn giao Medigo từ View báo cáo khớp 100% cột Google Sheets.
  4. **Bảng trạng thái chuẩn hóa (Status Reference)**: 
      - Bảng Markdown chuẩn hóa `4.4. Bảng trạng thái (Status Reference)` tương tự tài liệu mẫu `tracking_chi_phi_tp_vttd_m_session.md`.
      - Bảng gồm các cột: `Status`, `Tên`, `Diễn giải`, `Ai set`.
      - Mã trạng thái: `H` (Chờ duyệt - CRM set), `C` (Đã duyệt - Quản lý set), `R` (Từ chối - Quản lý set).
      - Thể hiện trực quan 2 luồng: `Luồng trạng thái chính: Chưa chốt → H → C` và `Luồng từ chối: H → R → H`.

---

## 6. Tinh chỉnh loại bỏ `status_chot` và `ly_do_tu_choi` ở root response
- **Lý do loại bỏ**:
  1. Trong Database Merap, tên cột chuẩn là `status` (nhận các giá trị chuẩn `H`, `C`, `R`), hoàn toàn không có cột nào tên là `status_chot` và không có trạng thái nào mang tên `CHUA_CHOT`.
  2. Bảng `tracking_chi_phi_tp_medigo_dang_ky` lưu trữ chi tiết theo từng Nhà thuốc với cặp khóa `(thang, custid)`. Do đó, `status` và `ly_do_tu_choi` là thuộc tính thuộc về **từng dòng bản ghi** trong mảng `data`, không phải thuộc tính ở cấp root.
  3. Ở các API ghi dữ liệu (API 3 và API 4), response trả về đã được chuẩn hóa giống mẫu `tracking_chi_phi_tp_vttd_m_session.md`: chỉ cần `status: "ok"` và `success_message`.

---

## 7. Tinh chỉnh Flat hóa dữ liệu dòng và bỏ `ma_chi_nhanh` trong API 1
- **Loại bỏ `ma_chi_nhanh` ở root**: Không cần thiết trả về ở tầng ngoài vì CRM đã được định danh qua `ma_crm` và `ten_crm`.
- **Flat hóa thông tin tháng cũ (`thong_tin_thang_cu`)**: Đưa các trường `so_luot_dang_ky_thang_cu`, `so_luot_su_dung_thang_cu`, `phan_tram_used`, `doanh_so_thang_cu_covat`, `doanh_so_ytd_covat` ra phẳng trực tiếp trong từng phần tử Nhà thuốc của mảng `data`. Giúp Frontend dễ dàng binding trực tiếp vào cột Table mà không cần parse qua object con.
- **Đồng bộ tên trường `dia_chi_nt`**: Thống nhất dùng `dia_chi_nt` ở cả API 1 và API 2.

---

## 8. Đánh giá toàn diện tính đầy đủ của API & Data Model
- **Về Database Schema**: Bảng `tracking_chi_phi_tp_medigo_dang_ky` với PK `(thang, custid)` và 20 cột đã bao phủ 100% yêu cầu người dùng, đủ các chỉ số đối soát (`so_luot_dang_ky`, `so_luot_su_dung`, `phan_tram_used`), trạng thái (`New`, `Renew`, `Stop`), workflow (`H`, `C`, `R`), và hệ thống log (`inserted_at`, `updated_at`, `approved_at`, `approved_by`, `rejected_at`).
- **Về View Báo cáo & Bàn giao**: View `v_tracking_chi_phi_tp_medigo_dang_ky_bao_cao` tự động mapping toàn bộ 15 trường thông tin phục vụ xuất file bàn giao Medigo và báo cáo BI.
- **Về Bộ API**: Bộ 6 API bao quát trọn vẹn toàn bộ vòng đời: Khởi tạo/Load form (API 1) $\rightarrow$ Tuyến thêm mới (API 2) $\rightarrow$ Chốt danh sách (API 3) $\rightarrow$ Phê duyệt/Từ chối (API 4) $\rightarrow$ Đối soát tự động (API 5) $\rightarrow$ Xuất file tích hợp (API 6).

---

## 9. Minh định rõ ràng Tháng Dữ Liệu và Phân Tách 2 Kịch Bản Output API 1
- **Kịch bản 1 (CHƯA CÓ Tháng 10 trong DB - Lần đầu CRM mở form)**:
  - Dữ liệu được SELECT trực tiếp từ các bản ghi Tháng 9 trong DB (`WHERE thang = '2026-09-01'`).
  - Trường `thang` trong từng dòng dữ liệu chính xác là **`"2026-09-01"`** (không fake thành tháng 10).
  - Các cột `so_luot_dang_ky`, `so_luot_su_dung`, `phan_tram_used` trong dòng này chính là dữ liệu nguyên bản của Tháng 9.
  - Frontend dựa vào `record.thang == thang_cu` để biết form đang ở chế độ xem lại đề xuất từ tháng trước.
- **Kịch bản 2 (ĐÃ CÓ Tháng 10 trong DB - CRM đã bấm Chốt danh sách)**:
  - Bản ghi đọc trực tiếp từ DB của Tháng 10, trường `thang = "2026-10-01"`.
  - Cột `so_luot_dang_ky` là số quota CRM đã chốt, mang trạng thái `status: 'H'` (hoặc `C`, `R`).
  - Các cột tháng 9 được tra cứu thêm để đối soát: `so_luot_dang_ky_thang_cu`, `so_luot_su_dung_thang_cu`, `phan_tram_used`.

---

## 10. Tối giản API 2 (Lấy tuyến Nhà thuốc khi thêm mới)
- Loại bỏ tham số `thang_dang_ky` khỏi input query params của API 2. Input chỉ còn duy nhất `{"manv": "..."}`.
- Backend tự động xác định tháng đăng ký mới nhất (ngày đầu tháng tiếp theo) để kiểm tra cờ `da_co_trong_danh_sach: true/false`.

---

## 11. Loại bỏ phụ thuộc bảng `d_hr_dsns`
- Chỉ sử dụng duy nhất bảng nhân sự `d_users` để tra cứu thông tin nhân viên (`manv`, `tencvbh`, `supid`).
- Loại bỏ hoàn toàn bảng `d_hr_dsns` khỏi phần thiết kế hệ thống và logic xử lý của API 1.

---

## 12. Nâng cấp API 3 cho phép sửa đổi linh động (DELETE nếu trùng key)
- Bổ sung cơ chế xử lý linh động cho phép CRM chỉnh sửa nhiều lần (chưa chốt, đang chờ duyệt `H`, hoặc sau khi bị từ chối `R`).
- Áp dụng logic chuẩn Merap: Nếu gặp trùng cặp khóa chính `(thang, custid)` thì thực hiện `DELETE` bản ghi cũ trước khi `INSERT` bản ghi mới, gán `status = 'H'`.
- Chỉ chặn khi danh sách đã được phê duyệt chính thức (`status = 'C'`).

---

## 13. Phê duyệt chi tiết theo cặp khóa `(thang, custid)` (API 4)
- Chuyển cơ chế phê duyệt của CRD/Quản lý từ cấp độ CRM sang chi tiết theo từng Nhà thuốc dựa trên cặp khóa chính `(thang, custid)`.
- Input nhận mảng các item có `thang`, `custid`, `action` (`APPROVE` / `REJECT`), `approved_by`, `ly_do_tu_choi`.
- Cho phép Quản lý duyệt/từ chối linh hoạt từng Nhà thuốc cụ thể hoặc chọn nhiều NT để duyệt hàng loạt.

---

## 14. Loại bỏ hoàn toàn API 5, API 6 và Phần 7
- **Loại bỏ API 5 (`insert_tracking_chi_phi_tp_medigo_sync_usage`) & API 6 (`get_tracking_chi_phi_tp_medigo_export`)**: Không cần thiết trong phạm vi module này.
- **Loại bỏ Phần 7 (Ràng buộc Nghiệp vụ & Nguyên tắc UX Frontend)**: Giữ tài liệu PRD tập trung 100% vào Data Schema, Business Logic và Core API Stored Functions của CRM & Quản lý (API 1, API 2, API 3, API 4).

---

## 15. Xác định cụ thể Cấp Phê duyệt: CRD – Anh Viển (`MR0045`)
- Cập nhật persona và workflow phê duyệt chính thức thuộc về CRD: Anh Viển (`MR0045`) / Admin.
- API 4 bổ sung điều kiện kiểm tra phân quyền: chỉ cho phép `approved_by = 'MR0045'` hoặc Admin thực hiện phê duyệt/từ chối.
- Cập nhật sample payload của API 4 sử dụng mã `MR0045`.

---

## 16. Chuẩn hóa Phần 4 thuần Business Rules & Xác định SĐT TDV do CRM tự điền
- **Chuyển Phần 4 thành Business Rules & Workflow thuần túy**: Loại bỏ toàn bộ các thuật ngữ chi tiết giao diện như "modal", "popup", "tab", v.v., chỉ mô tả logic nghiệp vụ, quy tắc kế thừa, kiểm duyệt và chốt dữ liệu.
- **Xác nhận SĐT TDV**: Hệ thống KHÔNG tự động điền SĐT TDV từ tuyến/users. Trường `sdt_tdv` bắt buộc do **CRM tự điền** số điện thoại của TDV phụ trách trực tiếp.
- Loại bỏ `sdt_tdv` khỏi output của API 2 (`get_tracking_chi_phi_tp_medigo_tuyen_nt`).

---

## 17. Bổ sung Quy tắc Duyệt từng phần & Cơ chế Auto-commit của IT/Admin
- **Cơ chế Auto-commit của IT/Admin (Đề xuất 1)**: Thêm ghi chú quy định mốc deadline hàng tháng (ví dụ: ngày 25). Nếu CRM quên chốt, Admin IT sẽ có script/job tự động quét kế thừa từ tháng cũ và chốt sang `H` để CRD duyệt kịp thời.
- **Quy tắc Duyệt từng phần & Chốt lại (Đề xuất 2)**:
  - Khi CRD duyệt từng phần (có NT `C` và NT `R`): các NT đã `C` bị khóa cứng không cho sửa.
  - API 3 nâng cấp logic bảo vệ: Kiểm tra nếu NT đã có `status = 'C'` trong DB thì tự động bỏ qua (không ghi đè/không xóa), chỉ xử lý `DELETE` và `INSERT` mới đối với các NT chưa có trong DB, đang ở `H`, hoặc bị từ chối `R`.
  - Đảm bảo an toàn 100% cho các NT đã được CRD duyệt khi CRM chốt lại danh sách.

---

## 18. Tạo bảng & Import dữ liệu từ Google Sheet vào PostgreSQL

**Thời điểm thực hiện**: 15/09/2026 ~15:32 (giờ địa phương)

### 18.1. DDL bảng chính
Tạo `public.tracking_chi_phi_tp_medigo_dang_ky` với PK `(thang, custid)` + index `(manv, thang)`.

### 18.2. Import dữ liệu tháng 09/2026
- **Nguồn**: Google Sheet `gid=1531697996`, Tab `Danh sách NT đăng ký`
- **Số bản ghi import**: **127 NT** (tháng `2026-09-01`)
- **Mapping**: `custid ← Mã KH`, `manv ← Mã CRM`, `custname ← Tên Nhà thuốc`, `thoi_gian_mo_cua`, `sdt_nha_thuoc`, `ten_dai_dien_nt`, `sdt_tdv`, `so_luot_dang_ky ← Số lượt tư vấn cài đặt`
- **Mặc định**: `trang_thai_nt = 'New'`, `status = 'C'` (tháng 9 là tháng baseline đã được duyệt chính thức)
- **Xử lý**: `ON CONFLICT (thang, custid) DO UPDATE` để idempotent

---

## 19. Triển khai 4 Stored Functions (APIs)

**Thời điểm thực hiện**: 15/09/2026 ~15:39 (giờ địa phương)

| # | Function Name | Loại | Mô tả |
|---|---|---|---|
| 1 | `get_tracking_chi_phi_tp_medigo_danh_sach(p_manv, p_thang_dang_ky)` | READ | Load form: Case 1 kế thừa tháng cũ / Case 2 load đã chốt, kèm doanh số |
| 2 | `get_tracking_chi_phi_tp_medigo_tuyen_nt(p_manv)` | READ | Lấy toàn bộ NT trong tuyến CRM + cờ `da_co_trong_danh_sach` + doanh số |
| 3 | `insert_tracking_chi_phi_tp_medigo_chot_danh_sach(p_data jsonb)` | WRITE | CRM chốt → DELETE+INSERT với `status='H'`; bảo vệ bản ghi `status='C'` |
| 4 | `insert_tracking_chi_phi_tp_medigo_phe_duyet(p_data jsonb)` | WRITE | CRD (`MR0045`) APPROVE/REJECT từng `(thang, custid)`, kiểm tra phân quyền |

**JOIN tables**: `d_users`, `api_f_thongtin_tuyen_mcp_tp_pcl`, `d_master_khachhang`, `f_raw_data_sales_yoy`

---

## 20. Kết quả Test tự động 4 APIs

**Thời điểm test**: 15/09/2026 ~15:41 (giờ địa phương)

| Test ID | Mô tả | Kết quả |
|---|---|---|
| 1a | API 1 - MR1156, tháng 10 (Case 1 - chưa có tháng 10) | ✅ PASS |
| 1b | API 1 - CRM không tồn tại → error | ✅ PASS |
| 1c | API 1 - MR0738, auto detect tháng (không truyền thang) | ✅ PASS |
| 2a | API 2 - MR1156 lấy 2,159 NT tuyến + cờ `da_co_trong_danh_sach` | ✅ PASS |
| 3a | API 3 - Chốt 2 NT mới tháng 10 | ✅ PASS |
| 3b | API 1 lại - Case 2 (đã có tháng 10), kèm so sánh tháng 9 | ✅ PASS |
| 3c | API 3 - Chốt lại NT đã `status=C` → bị bỏ qua | ✅ PASS |
| 4a | API 4 - MR0045 APPROVE + REJECT 2 NT, verify DB | ✅ PASS |
| 4b | API 4 - Non-CRD thử duyệt → từ chối phân quyền | ✅ PASS |

**→ Tổng hợp: 9/9 test cases PASS ✅**
